@model byte[]

@{
    var base64 = Convert.ToBase64String(Model);
    var imgsrc = string.Format("data:image/gif;base64, {0}", base64);
}
<img src="@imgsrc" style=" max-width: 300px; max-height: 400px" class="col-md-10" id="EditablePicture" />
